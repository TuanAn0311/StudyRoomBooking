# 1. THƯ MỤC THƯ VIỆN (Tuyệt đối không push vì rất nặng, người khác tải về sẽ tự chạy npm install)
node_modules/

# 2. FILE TẠM CỦA EXPO
.expo/
dist/
web-build/

# 3. FILE BUILD CỦA ANDROID (Chỉ giữ lại mã nguồn cấu hình, bỏ qua các file biên dịch rác)
android/app/build/
android/.gradle/
android/.idea/
android/app/.cxx/
*.apk
*.aab
*.keystore
*.jks

# 4. FILE BUILD CỦA IOS (Phòng hờ nếu sau này bạn chạy thêm iOS)
ios/Pods/
ios/build/
ios/Index/
*.xcworkspace/xcuserdata/

# 5. CẤU HÌNH IDE & CÔNG CỤ CÁ NHÂN (Tránh gây xung đột cấu hình với người chấm bài)
.vscode/
.claude/

# 6. FILE HỆ ĐIỀU HÀNH & LOGS
.DS_Store
Thumbs.db
npm-debug.log
yarn-error.log
yarn-debug.log